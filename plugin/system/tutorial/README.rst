A series of files in `plugin/system/tutorial/` include:

* `lj.py`, a module is developed to easily create LJ systems and analyze them.

* `test_1_lj_ref.py`, the LJ potential is compared against known values.

.. include:: /dev/sphinx/feedback.rst
